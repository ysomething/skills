# skills

Personal Codex skills repository.

Each skill lives in one top-level folder:

```text
skills/
  video-maker/
    SKILL.md
    agents/
    references/
    scripts/
```

Use lowercase hyphen-case folder names. Keep reusable helpers inside each skill folder so a skill can be copied or installed independently.
